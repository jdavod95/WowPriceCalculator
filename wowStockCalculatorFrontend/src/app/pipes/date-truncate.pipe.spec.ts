import { DateTruncatePipe } from './date-truncate.pipe';

describe('DateTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new DateTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
